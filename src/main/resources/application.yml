spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booksdb
    username: postgres
    password: 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  graphql:
    graphiql:
      enabled: true
